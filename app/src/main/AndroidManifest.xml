<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HunterQRHunter"
        tools:targetApi="31">
        <activity
            android:name=".page.EditProfileScreen"
            android:exported="false" />

        <activity
            android:name=".LoadingScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".page.MenuScreen" />
        <activity android:name=".page.QRCameraScreen" />
        <activity android:name=".page.UserScoresScreen" />
        <activity android:name=".page.QRMapScreen" />
        <activity android:name=".page.MyQRScreen" />
        <activity android:name=".page.QRScreen" />
        <activity android:name=".page.AddQRScreen"/>


    </application>

</manifest>